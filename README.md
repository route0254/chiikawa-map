# ちいかわ推し活（ちい活）MAP

GitHub Pages向けの静的Webサイトです。

公開URL: <https://chiikatsu-map.com/>

## ファイル構成

- `index.html` : 地図ページの画面構造
- `official.html` : 公式スポット一覧・店舗比較ページ
- `style.css` : デザイン / レスポンシブ対応
- `app.js` : 地図 / フィルター / 期間判定 / MarkerCluster / データ読込
- `official.css` / `official.js` : 公式一覧ページのデザイン / タブ / 分類 / 遅延読込
- `data/official-spots.json` : 現在開催中・今後開催のちいかわ公式関連スポット
- `data/official-events-archive.json` : 終了・開催中止となった公式イベント（必要時のみ読込）
- `data/nagano-spots.json` : ナガノ先生関連スポット
- `favicon.svg` : ファビコン
- `assets/ogp.png` : X / SNS共有用OGP画像
- `CNAME` : GitHub Pagesの独自ドメイン設定
- `scripts/validate-data.mjs` : スポットJSONの検証
- `scripts/check-links.mjs` : 公式情報リンクの定期検査
- `scripts/check-site-health.mjs` : 公開サイトのHTTPS・正規URL・主要ファイル検査
- `.github/workflows/validate-data.yml` : JSON検証のGitHub Actions
- `.github/workflows/test-site.yml` : JavaScript・JSON・主要UIのGitHub Actions
- `.github/workflows/check-links.yml` : 公式情報リンクの週次検査
- `.github/workflows/check-site-health.yml` : 公開サイト状態の週次検査
- `.github/ISSUE_TEMPLATE/` : 修正・掲載提案・終了報告用のIssueフォーム
- `tests/` : Playwrightによる主要UIのスモークテスト
- `package.json` / `pnpm-lock.yaml` : 開発・テスト用の依存関係
- `robots.txt` : 検索エンジン向けクロール設定
- `sitemap.xml` : 独自ドメインのサイトマップ
- `404.html` : 存在しないURL向け案内ページ
- `ROADMAP.md` : 今後のデータ・運用・機能課題
- `DATA-OPERATIONS.md` : データ確認・追加・終了処理の運用手順
- `docs/nagano-evidence-audit.md` : ナガセン関連の信憑性監査基準

## ローカルGit運用

作業開始時にGitHubの最新版を取得します。

```bash
git pull --ff-only origin main
```

初回のみ、開発・テスト用の依存関係とChromiumを準備します。公開サイト自体は従来どおり静的ファイルだけで動作します。

```bash
pnpm install
pnpm exec playwright install chromium
```

データやコードを変更したら、コミット前にJavaScript構文・JSON・主要UIを検証します。

```bash
pnpm run check
pnpm run test:smoke
```

Playwrightがローカルサーバーを自動起動し、通常表示・検索・一覧・スポット詳細・ダイアログ・スマホのタップ領域を確認します。
表示確認後、変更したファイルだけを指定してコミット・プッシュします。

```bash
git status
git add <変更したファイル>
git commit -m "変更内容の要約"
git push origin main
```

GitHub Pagesは `main` ブランチのルートから公開されます。`main` へのプッシュ後は自動的に公開処理が始まるため、ローカル検証を省略しないでください。
GitHub ActionsでもJSON検証と主要UIのスモークテストを行いますが、現在のPages設定では公開を停止するゲートではなく、GitHub上で異常を検知するための二重チェックです。
データ検証は毎週月曜日6時23分、外部リンク検査は同6時41分、公開サイト状態の検査は毎週水曜日7時17分（いずれも日本時間）に自動実行します。各検査はActions画面から手動実行もできます。

### 互換性を維持するルール

- 既存スポットの `id` は変更・再利用しない（`?spot=<id>` 形式の共有URLとの互換性を維持するため）
- `chiikawa-map-favorites-v1` と `chiikawa-map-visited-v1` のキー名・保存形式を変更しない
- 訪問日・メモは別キー `chiikawa-map-visit-details-v1` の任意データとして扱い、既存2キーへ混在させない
- 既存の共有URLパラメータ名や意味を変更しない
- JSONの既存フィールドを削除・改名しない
- `CNAME` の内容 `chiikatsu-map.com` を維持する
- 互換性を壊す変更が必要な場合は、移行処理を用意した別作業として扱う

## 今回の版

- 掲載基準日: 2026-08-28
- 公式関連（現在・今後）: 69件
- 公式過去イベント: 291件（開催中止1件を含む）
- ナガノ先生関連: 91件（確定 23件 / 推定 68件）
- 地図と相互移動できる「公式スポット一覧」ページを追加
- 公式一覧を「開催中・開催予定 / 過去の店舗・イベント / お店・施設の違い」の3タブで表示
- 開催中・予定は5種類、過去は3種類に分類し、過去JSONはタブ選択時のみ遅延読込
- 公式一覧で店舗・施設の種類、ブランド、開催状況、予約・入店方法、都道府県、保存状態を組み合わせて絞り込み可能
- 過去一覧で開催年、種類、都道府県、開催結果、予約・入店方法、保存状態を組み合わせて絞り込み可能
- 公式一覧のカードから「行きたい」「行った！」の登録・解除とスポット共有が可能
- 絞り込み候補の件数表示、並び順選択、過去一覧のブランド絞り込みに対応
- 開催中・開催予定一覧は都道府県順を初期表示とし、従来の順序は「開催状況・日付順」として選択可能
- 公式一覧の条件をURLへ保存し、再読込・ブラウザ履歴・共有で復元可能
- スマートフォンでは絞り込み・並び順を開閉式で表示
- タブ状態を `official.html?view=past` / `?view=guide` 形式で共有可能
- `spots.json` を公式関連 / ナガノ先生関連の2ファイルへ分割
- 2つのJSONは `app.js` で独立して読み込み、片方が失敗しても読み込めた側で表示を継続
- `OSHIKATSU MAP` → `CHIIKATSU MAP`
- `ちいかわ推し活MAP` → `ちいかわ推し活（ちい活）MAP`
- 「使い方」を「公式関連の使い方」「ナガセン関連の使い方」に分割
- ナガセン関連では `紹介 / 訪問 / ゆかり・関連` に加え、`確定 / 推定` の根拠区分を表示
- ナガセン関連の詳細画面に根拠説明・根拠リンクを追加
- 絞り込みに「ナガセン関連の確度（確定 / 推定）」を追加
- スポット名・住所・説明・シリーズ名を対象にした検索機能を追加
- 地図検索でひらがな・カタカナ、全角・半角、空白の表記差を吸収
- 検索候補をクリックすると該当スポットへ移動して詳細を表示
- 都道府県フィルターを追加（住所から選択肢を自動生成）
- ナガセンのピンを「確定 / 推定」で見た目から区別
- 日本語対応の `Zen Maru Gothic` を採用し、全体を丸みのある表示へ調整
- X / SNS共有用のOGPメタタグと `assets/ogp.png` を追加
- 「◎ 現在地」ボタンを追加。ブラウザの位置情報許可後、現在地を地図上に表示
- 「地図 / 一覧」表示切替を追加。検索・絞り込み結果を同じ条件のまま一覧表示
- スポット詳細・一覧から「♡ 行きたい」を保存可能
- 「♡ 行きたい」ボタンで保存済みスポットだけに絞り込み可能
- 行きたい情報は `localStorage` に保存し、ログイン・サーバー送信なし
- スマホ / タブレットでスポットを開いた際、地図下の詳細欄まで自動スクロール
- 単独ピンにスポット名を常時表示し、6件以下のクラスタには含まれるスポット名一覧を表示
- 一覧表示に「◎ 近い順」「▣ 地図範囲内」を追加
- スポット単体を `?spot=<spot-id>` 形式で共有可能
- 期間限定スポットに「本日終了」「あとN日」「明日から開催」等を表示
- 「⚠ まもなく終了（7日以内）」フィルターを追加
- 一覧の並び替えに「⌛ 終了が近い順」「あ 名前順」を追加
- 「✓ 行った！」を `localStorage` に保存し、訪問済みだけの絞り込みに対応
- 現在の検索・都道府県・各種フィルター・表示モードを共有できる「🔗 条件を共有」を追加
- 地図表示中は非表示の一覧カードを生成せず、一覧切替時にだけ描画
- 検索文字列・都道府県・フィルター状態の再計算を削減
- スポット詳細と各案内ダイアログのキーボード・読み上げ対応を改善
- スマホの主要操作領域を44px以上へ拡大
- データ読込・端末保存の失敗を画面上に表示し、データ読込には再試行導線を追加
- 行きたい・行った！をJSONでエクスポートし、現在の内容へ追加統合できるインポートを追加
- ツールバーの「💾 保存データ」から、バックアップ用の専用画面を開けるよう改善
- JSONの追加統合前に件数と競合を表示し、確認またはキャンセルできるプレビューを追加
- 同一座標の複数スポットは、2〜6件の件数別配置でピンの外側へ名称ラベルを分散し、接続線で対応関係を表示
- 同一地点の名称ラベル自体からも、対応するスポット詳細を開けるよう改善
- スポット詳細の「地図で開く」を「このスポットを共有」と並ぶボタンへ変更
- スポット詳細からGitHubの報告フォームへ対象情報を引き継ぐ導線を追加
- JavaScript構文・JSON・主要UIの自動検証を追加
- 「行った！」登録済みスポットに訪問日・メモを端末保存できる機能を追加
- スポット詳細に終了済みを除く近隣5件と直線距離を表示
- HTTPS・www転送・canonical・OGP・公開JSONの週次監視を追加
- 絞り込みに「過去イベント（終了・中止）」を追加し、初期状態では読み込まない遅延読込に対応
- 終了済みイベントを専用JSONへ分離し、既存IDと共有URLを維持
- 過去イベントの開催年絞り込みと、`past=1&year=2021` 形式の条件共有に対応
- 公式履歴一覧から国内候補を生成する調査用スクリプトを追加し、2021～2022年の61件を追加
- 2023年10～12月の国内公式POP UP STORE 13件を追加し、過去イベントを95件へ拡充
- 2023年7～9月の国内公式POP UP STORE 11件を追加し、過去イベントを106件へ拡充
- 2023年4～6月の国内公式POP UP STORE等14件を追加し、過去イベントを120件へ拡充
- 2023年1～3月の国内公式POP UP STORE 13件を追加し、過去イベントを133件へ拡充
- 2024年1～3月の国内公式POP UP STORE 13件を追加し、過去イベントを146件へ拡充
- 2024年6月開始の国内公式POP UP STORE 8件を追加し、過去イベントを154件へ拡充
- 2024年5月開始の国内公式POP UP STORE 9件を追加し、過去イベントを163件へ拡充
- 2024年4月開始の国内公式POP UP STORE 8件を追加し、過去イベントを171件へ拡充
- 2024年7～9月開始の国内公式イベント33件を追加し、過去イベントを204件へ拡充
- 2024年10～12月開始の国内公式イベント32件を追加し、過去イベントを236件へ拡充
- 2025年1～3月開始の国内公式イベント27件を追加し、過去イベントを263件へ拡充
- 2025年4～6月開始の国内公式イベント28件を追加し、過去イベントを291件へ拡充
- 2025年7～9月開始の国内公式イベント28件を追加し、過去イベントを319件へ拡充
- 2025年10～12月開始の国内公式イベント21件を追加し、過去イベントを340件へ拡充
- 2026年1～3月開始の国内公式イベント14件を追加し、過去イベントを354件へ拡充
- 開催中止イベントを赤い「開催中止」表示と注意文で区別
- 公式店舗・イベントの専用一覧ページを追加し、地図ページとの相互ナビゲーションに対応
- 常設ショップ、常設フード・体験、期間限定ショップ、期間限定カフェ、イベント・体験に分類
- 過去イベントの年・種類・都道府県・開催結果・予約方法・保存状態・キーワード絞り込みと遅延読込に対応
- 12種類の公式店舗・施設について、主な楽しみ方と営業形態の違いを比較表示


## 更新履歴

このセクションでは、このプロジェクトでこれまで実施した主な更新を時系列でまとめています。
同日に複数回更新しているため、日付だけでなく内容ごとに段階を分けています。

### 2026-08-28 公式イベント履歴追加

- 2025年1～3月開始の国内公式POP UP STORE 25件とレストランドリンクスタンド2件を追加
- 新規9会場の公式住所と国土地理院座標を確認し、残る18会場は既存の監査済みデータを再利用
- 公式過去イベントを263件へ拡充し、国内の未登録候補を119件へ更新
- 2025年4～6月開始の国内公式POP UP STORE 27件とレストランドリンクスタンド1件を追加
- 新規6会場の公式住所と国土地理院座標を確認し、残る22会場は既存の監査済みデータを再利用
- 公式過去イベントを291件へ拡充し、国内の未登録候補を91件へ更新
- 2025年7～9月開始の国内公式POP UP STORE 27件とレストランドリンクスタンド1件を追加
- 新規7会場の公式住所と国土地理院座標を確認し、残る21会場は既存の監査済みデータを再利用
- 公式過去イベントを319件へ拡充し、国内の未登録候補を63件へ更新
- 2025年10～12月開始の国内公式POP UP STORE 21件を追加
- 新規9会場の公式住所と国土地理院等の座標を確認し、残る11件は既存の監査済み会場データを再利用
- 公式過去イベントを340件へ拡充し、国内の未登録候補を42件へ更新
- 2026年1～3月開始の国内公式POP UP STORE 14件を追加
- 新規4会場の公式住所と国土地理院等の座標を確認し、残る10会場は既存の監査済みデータを再利用
- 公式過去イベントを354件へ拡充し、国内の未登録候補を28件へ更新

### 2026-08-26 アクセシビリティ・保存・検証改善

- 詳細表示へ移動したフォーカスを、閉じた際に元の検索欄または地図操作へ戻すよう改善
- 絞り込み・使い方ダイアログにフォーカストラップとモーダル属性を追加
- スマホの主要ボタン、検索、選択、地図ズーム操作を44px以上へ調整
- 地図モードでは一覧カードDOMを破棄し、一覧モード時だけ生成するよう変更
- スポット読込時に検索用文字列と都道府県を作成し、絞り込み処理を軽量化
- JSON読込失敗とlocalStorage保存失敗を画面上で案内
- 行きたい・行った！のJSONエクスポート／追加統合インポートを追加
- 保存データ操作を絞り込みパネルから独立させ、ツールバーの「💾 保存データ」から開く専用画面へ変更
- 同一座標にある複数スポットのピンと名称ラベルを、2〜6件の件数別固定パターンで外側へ分散して重なりと線の交差を抑制
- 分散した名称と対応するピンを接続線で示し、名称ラベル自体も詳細を開く操作に対応
- スポット詳細の「地図で開く」を、共有ボタンと同じ操作エリアにあるボタン表示へ変更
- スポット詳細から対象情報を入力済みのGitHub Issueフォームへ移動できる報告リンクを追加
- PlaywrightスモークテストとGitHub Actionsを追加
- 適用中の検索・絞り込み条件を画面上に表示し、1操作ですべて解除できるよう改善
- 検索・絞り込み結果が0件の際に、全件表示へ戻す案内と解除ボタンを表示
- 公式情報URLの404・410、アクセス制限、HTTPリンクを週次確認する外部リンク検査を追加
- GitHub ActionsをNode.js 24ランタイム対応版へ更新
- 訪問日・メモを既存の「行った！」データと分離して端末保存し、JSONバックアップにも追加
- 旧形式のバックアップを引き続き読み込める互換性を維持し、訪問記録の競合時は端末側の入力済み項目を優先
- スポット詳細に、終了済みを除く近隣5件を直線距離順で表示する導線を追加
- 公開サイトのHTTPS、HTTP・www転送、canonical、OGP、主要ファイルを毎週確認するGitHub Actionsを追加
- 過去イベントを通常表示では読み込まず、絞り込みで有効化した場合だけ検索・地図・一覧に追加
- 終了済みイベントの共有URLを開いた場合はアーカイブを自動読込して詳細を表示
- データ更新運用とナガセン関連の信憑性監査基準を文書化
- 公式履歴一覧・カフェ・展覧会の計393件から国内のアーカイブ候補を機械抽出する調査フローを追加
- 2021～2022年の国内公式イベント61件を追加し、公式履歴上の最古記録である2021年まで収録範囲を拡大
- 開催中止イベント1件をアーカイブへ追加し、一覧・地図・詳細で「開催中止」と明示
- 過去イベントの開催年絞り込みと共有URLへの開催年保存を追加
- X / SNS共有用OGP画像を更新

### 2026-08-24 初期版

- GitHub Pagesで公開する静的サイトとして作成
- `HTML / CSS / JavaScript` のみで構成し、React・Node.js・DB・サーバーサイド処理は使用しない方針を採用
- Leaflet 1.9.4 を導入
- 背景地図に OpenStreetMap を採用
- スポット情報をJSONから読み込む方式を採用
- ちいかわ公式ショップ・施設・コラボ・POP UP・イベント・グルメ等を地図上に表示
- 公式系スポットを約70件登録
- 期間限定スポットに `startDate / endDate` を持たせ、日本時間基準で終了済みを自動非表示
- 営業時間・定休日・予約・整理券・入場方法・情報元・Google Mapsリンク等を詳細画面に表示

### 2026-08-24 フィルター・データ設計改善

- 「関連」フィルターを追加
  - ちいかわ公式
  - ナガノ先生
- 「スポットの種類」フィルターを追加
  - ショップ
  - グルメ
  - おでかけ
  - 宿泊
  - その他
- 「公式関連の種類」フィルターを追加
  - 公式ショップ
  - 公式施設
  - コラボ
  - POP UP
  - イベント・展示
- 「ナガノ先生関連の種類」フィルターを追加
  - 紹介
  - 訪問
  - ゆかり・関連
- 「開催期間」フィルターを追加
  - 常設
  - 期間限定
- 「予約」フィルターを追加
  - 予約不可
  - 予約可
  - 予約優先
  - 要予約
  - 要確認
- `brand` を使った「シリーズ・施設」フィルターを追加
- ブランド一覧は `spots.json` の内容から自動生成する方式に変更
- 「すべて表示に戻す」ボタンを追加

### 2026-08-24 地図表示・障害耐性改善

- Leaflet MarkerCluster 1.5.3 を導入
- 広域表示では近接スポットをクラスタ化
- 同一・近接座標の複数スポットは最大ズーム時に Spiderfy で展開
- MarkerClusterが読み込めない場合、通常の `L.layerGroup()` へ自動フォールバック
- 地図本体に `maxZoom: 20` を設定し、MarkerClusterとの互換性を改善
- OSM公式タイルは `maxNativeZoom: 19` とし、ズーム20では19のタイルを拡大表示
- OpenStreetMap障害時に Stadia Maps へ自動切替するフォールバックを実装
- 5秒以内に3タイル以上失敗した場合を障害判定とする処理を追加
- OSM / 全背景地図の障害試験用URLを追加
- すべての背景地図が失敗してもスポット情報自体は利用できるようにした

### 2026-08-24 初期表示・スマホUI改善

- 東京固定の初期表示をやめ、終了済みを除く全登録スポットが収まる `fitBounds()` に変更
- PC / スマホで `fitBounds` の余白を調整
- フィルター変更時には自動 `fitBounds()` を行わず、ユーザーが見ている位置を維持
- Leaflet標準Popupを廃止
- PCではスポット詳細を地図右側に表示
- スマホではスポット詳細を地図下に表示
- ピンタップ時に地図やページが勝手にパン・スクロールしないよう変更
- 地図をカード型レイアウトに変更
- PC / スマホ / 小型スマホで地図高さをレスポンシブ調整

### 2026-08-24 デザイン・案内改善

- ピンク・ミント・薄紫・白を中心とした配色に変更
- 公式スポットをピンク、ナガノ先生関連をグリーンで表現
- 公式＋ナガノ混在クラスタはピンク～紫～グリーンのグラデーション表示
- サイト上部に掲載基準日・非公式ファンサイト・最新公式情報確認の注意書きを追加
- GitHub Issuesへの「掲載内容について連絡」リンクを追加
- 地図上部に「使い方」パネルを追加
- 予約と入場制限が別概念であることを説明
- `favicon.svg` を追加し `/favicon.ico 404` を解消
- 外部URLは `http:` / `https:` のみ許可する処理を追加
- 外部リンクに `target="_blank"` / `rel="noopener noreferrer"` を付与
- JSON取得を `cache: "no-store"` に変更し、古いデータのブラウザキャッシュを抑制

### 2026-08-24 公式・ナガセンデータ分割

- 旧 `data/spots.json` を以下の2ファイルに分割
  - `data/official-spots.json`
  - `data/nagano-spots.json`
- 2つのJSONを独立して読み込む方式へ変更
- 片方のJSON読込に失敗しても、読み込めた側だけで地図表示を継続
- ナガノマーケットは「ナガノ先生本人関連」ではなく公式カテゴリとして維持
- ナガノ先生本人の紹介・訪問・ゆかりスポット用データ構造を追加

### ナガセン関連スポット

- ナガノ先生関連スポットを91件登録
  - 確定 23件
  - 推定 68件
- `introduced / visited / related` で関係性を分類
- `evidenceStatus` を追加
  - `confirmed` : 確定
  - `inferred` : 推定
- `evidenceNote` / `evidenceUrl` / `evidenceCheckedAt` を追加
- スポット詳細に「確定 / 推定」表示と根拠説明・根拠リンクを追加
- 絞り込みに「ナガセン関連の確度（確定 / 推定）」を追加
- 「推定」は事実断定ではなく巡礼候補として掲載する方針を明記
- 「確定 / 推定」はナガノ先生との関係性・場所特定の確度であり、営業状況の確度ではない旨を明記

### 2026-08-24 表記・使い方再設計

- `OSHIKATSU MAP` を `CHIIKATSU MAP` に変更
- サイト名を「ちいかわ推し活MAP」から「ちいかわ推し活（ちい活）MAP」に変更
- 「使い方」を以下の2つに分割
  - 公式関連の使い方
  - ナガセン関連の使い方
- ナガセン関連の掲載基準・確定 / 推定の考え方を使い方内へ追加

### 2026-08-24 検索・都道府県・デザイン強化

- スポット検索機能を追加
- 検索対象をスポット名・住所・説明・シリーズ / 施設名等へ拡張
- 入力中に検索候補を表示し、選択すると該当スポットへ移動して詳細表示
- 検索と既存フィルターをAND条件で併用可能にした
- 都道府県フィルターを追加
- 都道府県の選択肢はスポット住所から自動生成
- 日本語対応フォント `Zen Maru Gothic` を採用
- タイトル・見出しを中心に丸みのあるデザインへ調整
- ナガセン確定 / 推定を地図上のピンでも区別
- X / SNS共有用OGP画像 `assets/ogp.png` を追加
- `og:*` / `twitter:*` メタタグを追加
- OGP画像サイズ・MIME・alt・site_name等を追加してOGP情報を強化
- OGPキャッシュ対策として画像URLにバージョンパラメータを付与

### 2026-08-24 現在地・一覧・行きたい機能

- 「◎ 現在地」ボタンを追加
- Geolocation APIを利用して現在地へ移動し、現在地マーカーと精度範囲を表示
- 位置情報はブラウザ上の地図表示にのみ利用し、サーバーへ保存・送信しない設計
- 「地図 / 一覧」表示切替を追加
- 検索・都道府県・各種絞り込み条件を維持したまま一覧表示へ切替可能
- 一覧から該当スポットの地図・詳細へ移動可能
- 「♡ 行きたい」機能を追加
- スポット詳細・一覧の両方から行きたい登録 / 解除が可能
- 「♡ 行きたい」登録済みスポットだけを表示するフィルターを追加
- 行きたい情報は `localStorage` のみに保存し、ログイン・サーバー送信なし

### 2026-08-24 モバイル詳細・地図ラベル改善

- スマホ / タブレット幅では、ピンや検索結果からスポットを開いた際に詳細欄まで自動スムーズスクロール
- PCでは従来どおり右側詳細表示のため自動スクロールしない
- 単独ピンの近くにスポット名を常時表示
- 6件以下のクラスタでは、クラスタ近くに含まれるスポット名を1行ずつ表示
- 7件以上のクラスタは件数のみ表示し、地図上の文字量を抑制

### 2026-08-24 近い順・地図範囲内・スポット共有

- 一覧表示に「◎ 近い順」を追加
- 現在地から各スポットまでの概算距離を計算し、距離順に並び替え
- 一覧に各スポットまでの概算距離を表示
- 「▣ 地図範囲内」を追加
- 既存の検索・都道府県・カテゴリ・行きたい等で絞り込んだ結果に対して、最後に現在の地図表示範囲を追加条件として適用
- スポット詳細・一覧に「このスポットを共有」を追加
- `?spot=<spot-id>` 形式の共有URLを生成
- 共有URLを開くと該当スポットへ自動ズームして詳細を表示
- Web Share API対応環境では端末の共有メニューを利用し、非対応環境ではURLコピーにフォールバック

### 2026-08-24 期間・訪問記録・条件共有

- 期間限定スポットに残り日数・開催予定表示を追加
  - 本日終了
  - あとN日
  - 明日から開催
  - あとN日で開催
- 「⚠ まもなく終了（7日以内）」絞り込みを追加
- 一覧の並び替えを追加
  - ◎ 近い順
  - ⌛ 終了が近い順
  - あ 名前順
- 並び替えは1種類ずつ切り替える方式にし、条件競合を防止
- 「✓ 行った！」機能を追加
- 詳細・一覧から訪問済み登録 / 解除が可能
- 「✓ 行った」登録済みだけを表示するフィルターを追加
- 訪問済み情報も `localStorage` のみに保存し、サーバー送信なし
- 「🔗 条件を共有」を追加
- 検索語・都道府県・各種フィルター・まもなく終了・地図 / 一覧表示をURLへ保存可能
- 条件共有URLを開くと共有時の表示条件を自動復元
- 現在地・距離順・地図範囲・行きたい・行った等の端末固有情報は共有URLに含めない
- スポット単体共有URL `?spot=<id>` と条件共有URLを分離し、既存機能との競合を防止

## 端末への保存について

「♡ 行きたい」「✓ 行った！」と、行ったスポットに入力した訪問日・メモはブラウザの `localStorage` を利用します。
アカウント登録やサーバーへの保存・送信はありません。
そのため、別ブラウザ・別端末・シークレットモードなどとは共有されず、ブラウザデータを削除すると保存内容も消えます。

ツールバーの「💾 保存データ」を開き、「↓ JSONを書き出す」でJSONファイルへバックアップできます。
別端末などで「↑ JSONを読み込む」を選ぶと、その端末にある現在の保存内容を消さずに追加統合します。
既存の保存キー `chiikawa-map-favorites-v1` / `chiikawa-map-visited-v1` と配列形式は維持しています。訪問日・メモのみ、別キー `chiikawa-map-visit-details-v1` にスポットID単位で保存します。

旧版が書き出した訪問記録を含まないJSONも、そのまま読み込めます。読み込み時は追加件数と競合件数を確認してから統合できます。新しいJSONの訪問記録と端末側の内容が競合した場合は、誤上書きを避けるため端末側の入力済み項目を優先し、空欄だけを補います。
「行った！」を解除しても訪問日・メモは誤消去防止のため保持し、再登録すると再表示します。完全に削除する場合は詳細内の「日付・メモを消去」を使用します。

現在地機能はブラウザの Geolocation API を利用し、取得した位置情報をサーバーへ保存・送信する処理はありません。

## データ更新時

### 更新手順

1. `git pull --ff-only origin main` で最新版を取得
2. 対象のJSONを編集
3. 情報を確認した日付と、必要に応じて `app.js` の `DATA_AS_OF` を更新
4. `pnpm run check` を実行
5. URLを変更した場合は `pnpm run check:links` を実行
6. 公開ドメイン設定やメタ情報を変更した場合は `pnpm run check:site` を実行
7. `pnpm run test:smoke` で表示・検索・絞り込み・詳細を確認
8. 変更したファイルだけをコミットして `main` へプッシュ

検証スクリプトは、JSON構文、フィールド構造、必須項目、ID重複、座標、日付、列挙値、URLを確認します。
エラーが1件でもある場合は終了コード1になり、修正するまでプッシュしません。
期間限定スポットの終了日不足やHTTP URLは警告として表示します。警告は内容を確認し、根拠なしに日付やURLを推測して埋めないでください。

`hoursCheckedAt` と `entryInfoCheckedAt` は90日、`evidenceCheckedAt` は365日を超えると再確認の警告を表示します。
GitHub Actions上では警告をアノテーションとして表示し、データ更新が必要な箇所を見つけやすくします。
終了日まで14日以内の期間限定スポットも確認情報として表示します。

`pnpm run check:links` は営業時間・入店方法・予約・根拠・情報元URLへ実際に接続して確認します。
404・410は破損リンクとして終了コード1、403・429・タイムアウトなどはBot制限や一時障害の可能性があるため要確認として扱います。
GoogleマップURLは通常の検査対象から除外しており、必要な場合だけ `node scripts/check-links.mjs --include-map` で確認できます。
同じ検査は `.github/workflows/check-links.yml` により毎週自動実行され、公開サイトのデプロイ処理とは独立しています。

`pnpm run check:site` は公開URLへ接続し、HTTPS証明書、HTTPからHTTPSへの転送、wwwから正規ドメインへの転送、canonical、OGP URL、`app.js`、3つのスポットJSONを確認します。
同じ検査は `.github/workflows/check-site-health.yml` により毎週自動実行されます。2026-08-26時点で `https://chiikatsu-map.com/` は200、HTTPとwwwは正規HTTPS URLへ301転送されます。

`pnpm run report:status` は、現在の公式スポットから「終了日を過ぎたアーカイブ移動候補」「14日以内に終了するスポット」「終了日未定の期間限定スポット」を抽出します。候補を自動更新するコマンドではないため、JSONを変更する前に必ず公式情報を確認してください。

公式情報に「期間限定・終了日未定」と明記されている場合は、`periodType` を `limited`、`endDate` を `null` のまま維持できます。
確認済みスポットは `scripts/validate-data.mjs` の `confirmedOpenEndedLimitedSpotIds` にIDを登録し、警告ではなく確認情報として表示します。
終了日が発表された時点で `endDate` を更新し、許容リストからIDを削除してください。

### 1. 公式関連
現在開催中・今後開催は`data/official-spots.json`を更新します。
終了・開催中止となったイベントは、同じ`id`を維持したまま`data/official-events-archive.json`へ移します。詳しい確認頻度・掲載基準・移動手順と履歴調査フローは`DATA-OPERATIONS.md`を参照してください。

### 2. ナガノ先生関連
`data/nagano-spots.json` を更新します。

`category` は以下を使用します。

```json
"category": "nagano"
```

`relationType` は以下です。

- `introduced` : ナガノ先生が紹介
- `visited` : ナガノ先生が訪問
- `related` : ナガノ先生ゆかり・関連

ナガセン関連では、根拠の状態も以下のフィールドで管理します。

```json
"evidenceStatus": "confirmed",
"evidenceNote": "確定・推定と判断した理由",
"evidenceUrl": "https://...",
"evidenceCheckedAt": "2026-08-24"
```

- `confirmed` : **確定**。本人SNS、作品内の明示、出版社・店舗・自治体などの公式情報から、その店・場所との関係を直接確認できるもの
- `inferred` : **推定**。作品描写、商品、外観、旅程などとの一致から特定度は高いが、一次情報だけでは店舗名・地点を完全には断定できないもの

「推定」は断定情報ではなく巡礼候補として掲載します。噂だけのものや候補を一つに絞れないものは登録しません。

※ `confirmed / inferred` はナガノ先生との関係性・場所特定の確度です。現在の営業状況や営業時間の確度を表すものではありません。

### 3. 掲載基準日
`app.js` 冒頭の以下を変更してください。

```js
const DATA_AS_OF = "2026-08-27";
```

### 4. 新しいシリーズ・施設を追加した場合
`app.js` の `BRAND_LABELS` に表示名を追加すると、「シリーズ・施設」フィルターへ自動表示されます。
あわせて `scripts/validate-data.mjs` の `officialBrands` に同じキーを追加し、JSON検証で許可してください。

## テストURL

通常:
`https://chiikatsu-map.com/`

OSM障害テスト:
`https://chiikatsu-map.com/?tileTest=osm-fail`

全背景地図障害テスト:
`https://chiikatsu-map.com/?tileTest=all-fail`

## 注意

本サイトは非公式の個人的なファンまとめを想定しています。
営業時間・入場方法・予約条件・開催状況は変更される場合があるため、訪問前に各公式情報も確認してください。
