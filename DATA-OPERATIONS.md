# ちい活マップ データ更新運用

最終更新: 2026-09-01

## 目的

現在・過去の公式スポットと、ナガセン関連の根拠を更新するための手順です。実コードとデータを正とし、既存ID・共有URL・端末保存との互換性を維持します。

## 担当範囲

| 区分 | 内容 |
|---|---|
| サイト管理 | 公式情報の確認、掲載判断、データ更新、テスト、公開確認 |
| GitHub Actions | JSON検証、公式スポット状況サマリー、更新漏れ検知、外部リンク検査、公開サイトのHTTPS・正規URL・主要ファイル確認 |

掲載可否や確定・推定の判断が分かれる情報は、根拠と判断理由を記録してから反映します。

## データファイル

- `data/official-spots.json`: 現在開催中・今後開催の公式関連
- `data/official-events-archive.json`: 終了・開催中止となった公式イベント
- `data/nagano-spots.json`: ナガセン関連

過去イベントJSONは通常アクセスでは読み込まず、絞り込みの「過去イベント（終了・中止）」または過去イベントの共有URLを開いた場合だけ読み込みます。読み込み後は開催年でも絞り込めます。

## 更新頻度

### 随時

- 新規イベント、施設、コラボの発表
- 開催期間の変更・延長
- 閉店・移転・開催終了
- 公式URL、営業時間、入場方法の変更
- GitHub Issueからの修正・追加提案

### 毎日・毎週・自動

- 毎日6時23分: JSON・日付・必須項目を検証し、終了日経過・終了間近・終了日未定をJob Summaryへ一覧化
- 月曜日6時41分: 公式情報リンクの確認
- 毎日7時17分: HTTPS・リダイレクト・canonical・OGP・公開JSONの確認

### 月1回・手動調査

- `pnpm run report:status` による終了・移動候補の確認
- 公式サイト・公式SNSの新規情報巡回
- 終了日未定の期間限定スポット追跡
- 期間終了データのアーカイブ移動
- 古くなった営業時間・入場情報確認日の更新
- ナガセン根拠リンクと営業状況の確認
- 未対応Issueの確認

## 公式関連の更新手順

1. 公式サイト、公式SNS、主催施設等の一次情報を確認する
2. 既存IDとの重複、同一イベントの別会場かどうかを確認する
3. 新規データまたは変更案を作成する
4. 終了日・会場・根拠が曖昧な場合は推測せず、確認事項として報告する
5. JSONを変更した場合は`pnpm run build:spot-pages`を実行する
6. `pnpm run check`を実行する
7. URL変更時は`pnpm run check:links`を実行する
8. `pnpm run test:smoke`を実行する
9. コミット・プッシュ後にGitHub Actionsと公開サイトを確認する

`spot/<id>/index.html`と`sitemap.xml`は生成物です。直接編集せず、表示変更は`scripts/build-spot-pages.mjs`、`spot.css`、`spot-page.js`で行います。GitHub ActionsでもJSONと生成物の差異を検出します。

## 過去イベントの掲載基準

次のいずれかでイベント名・会場・期間を確認できるものを掲載します。

1. ちいかわ公式サイト・公式SNS
2. 主催施設、店舗、自治体の公式発表
3. 出版社・ライセンス元の公式発表
4. 削除済み公式ページのWebアーカイブ

ファン投稿だけを根拠にした情報、会場または期間を特定できない情報は掲載しません。

掲載対象は国内会場に限定します。海外開催は調査候補に残しても、公開JSONへは追加しません。

開催予定だったものの公式情報で中止を確認できる場合は、履歴として掲載できます。その際は `eventStatus: "cancelled"` を設定し、`hoursText`・`entryNote`・`description`にも実際には開催されなかったことを明記します。通常開催されたイベントは `eventStatus: "held"` とします。既存データとの互換性のため、フィールドがない過去データは `held` として扱います。

`official-events-archive.json`には、`periodType`が`limited`で、実在する`endDate`があり、その終了日が今日より前のデータだけを登録します。検証スクリプトがこの条件を強制します。

## 公式履歴の一括調査

大量の履歴追加では、公式履歴ページを1件ずつ手作業で転記せず、`research/`の調査スナップショットと次のコマンドを利用します。

1. 公式のPOP UP STORE・カフェ・展覧会履歴を確認し、`research/official-history-source.json`を更新する
2. `pnpm run build:history-candidates`で国内判定、終了判定、登録済みURLとの照合を行う
3. 未登録候補を年単位で分け、会場名・住所・開催期間・公式URLを確認する
4. 会場住所を`research/history-venue-seeds-*.json`へ登録する
5. 座標は既存データを優先し、不足分だけNominatimまたは国土地理院住所検索APIで低頻度に取得してキャッシュする
6. 調査用バッチJSONを目視確認し、`pnpm run check`と`pnpm run test:smoke`を通してからアーカイブへ統合する

公式サイト側のアクセス制限により、履歴ページの取得はブラウザで表示内容を確認してスナップショット化します。公開サイトは外部APIへ接続せず、確定したJSONだけを読み込みます。座標取得は各サービスの利用条件を守り、キャッシュを再利用して同じ住所へ繰り返し問い合わせません。詳細は`research/README.md`を参照してください。

### 特設ページ系列イベントの一括更新

POP UP STORE・カフェ・展覧会の3一覧に含まれない系列イベントは、`research/official-special-events-source.json`を確認済み原本として管理します。

1. 国内の実会場、会場別期間、公式URL、住所、座標を一次情報で確認する
2. 系列と会場を原本へ追加し、`pnpm run build:special-events`で現在・過去の振り分けと重複をプレビューする
3. 内容を確認して`pnpm run import:special-events`で2つの公開JSONへ反映する
4. `pnpm run build:spot-pages`と`pnpm run check`を実行する

振り分け基準日は原本の`checkedAt`です。`endDate`が基準日より前なら過去、それ以外は現在・今後として生成します。原本で管理するIDは再実行時も維持され、基準日更新後は同じIDのまま現在JSONから過去JSONへ移動します。`pnpm run check`は原本の管理対象が正しいJSONに同期していることと、既存データとのID・系列・期間・会場の重複を検査します。

## 開催終了時の移動手順

1. `pnpm run report:status` で終了日経過候補を抽出する
2. 公式情報で終了を確認する
3. `id`を変更せず、オブジェクトを`official-spots.json`から`official-events-archive.json`へ移す
4. 「現在の地図では非表示」など移動前の状態に依存した説明を修正する
5. 共有URL `?spot=<id>` から地図詳細が開き、`/spot/<id>/` 個別ページも生成されていることを確認する
6. 「行きたい」「行った！」「訪問記録」のID互換性を確認する

`report:status` は候補を表示するだけで、JSONを自動変更しません。終了日当日までは開催中として扱い、終了日より後になった項目だけをアーカイブ移動候補として表示します。

毎日のGitHub Actionsも同じ判定を行います。終了間近と終了日未定は確認情報として通知し、終了日経過後も`official-spots.json`に残る項目が1件以上ある場合は更新漏れとしてActionを失敗にします。失敗してもデータを自動移動せず、必ず公式情報を確認してから上記手順で更新します。

IDを維持するため、移動後も既存共有URLとlocalStorageはそのまま利用できます。

## 終了日未定の期間限定スポット

公式情報に終了日未定と明記されている場合だけ`endDate: null`を許容します。対象IDは`validate-data.mjs`の許容リストへ登録し、月1回以上終了日発表を確認します。推測日を入力してはいけません。

## ナガセン関連

公開判定は現在の`confirmed`／`inferred`を維持します。再調査では[ナガセン関連 信憑性監査](docs/nagano-evidence-audit.md)に従い、10～15件ずつ調査・報告・承認・反映します。

`relationType`は重複しない主分類として扱います。現地訪問・飲食・購入・施設利用を確認できる場合は`visited`、紹介は確認できるが訪問までは確認できない場合だけ`introduced`、訪問・紹介とは別の制作・公的関与は`related`とします。ナガノマーケット、ナガノ展、ナガノの水族館などの公式店舗・イベントは公式関連データの対象とし、ナガセンへ二重登録しません。

通常の絞り込みは、`placeType`による目的別と3段階の確度を組み合わせます。`evidenceStatus: confirmed`は「確定」、`evidenceStatus: inferred`かつ`evidenceNote`冒頭が「【推定・高確度】」のものは「推定・高確度」、それ以外の`inferred`は安全側に倒して「要注意候補」と表示します。旧`evidence=inferred`共有URLは後者2つへ展開し、`relationType`は詳細表示と旧URL互換のため削除しません。

## GitHub Actions失敗時の対応

| 検査 | 主な意味 | 最初に行うこと |
|---|---|---|
| Validate spot data | JSON不正、必須項目不足、重複ID、または終了済み候補の残存 | ログとJob Summaryで対象IDを確認し、公式情報とJSONを照合する |
| Check official information links | 公式情報URLの404・410、接続制限、一時障害 | 対象URLをブラウザで開き、移転先URLまたは一時障害かを確認する |
| Test site | JavaScript構文、JSON、主要UIの回帰 | 最初に失敗したステップとPlaywrightの対象テストを確認する |
| Check public site health | HTTPS、転送、canonical、OGP、公開ファイルの異常 | Pagesの直近デプロイと公開URLを確認し、DNS変更は原因確認前に行わない |

Actionsの失敗は自動修正の合図ではなく、調査開始の合図として扱います。外部サイトの一時障害やBot制限もあるため、ログだけでデータを削除・変更しません。

## 変更前後の必須確認

- 既存IDを変更・再利用していない
- JSONの既存フィールドを削除・改名していない
- `chiikawa-map-favorites-v1`、`chiikawa-map-visited-v1`、`chiikawa-map-visit-details-v1`を壊していない
- `?spot=`と条件共有URLを壊していない
- `pnpm run build:spot-pages` 後に個別ページとサイトマップの差分を確認した
- 通常アクセスでは過去イベントが初期非表示
- 過去イベントを有効にすると検索・地図・一覧へ追加される
- 開催年を指定すると、その年に開始した過去イベントだけが表示される
- 開催中止イベントが赤い表示と注意文で明確に区別される
- PC・スマホ双方で主要操作を利用できる
- CNAME、DNS、GitHub Pages設定を変更していない
